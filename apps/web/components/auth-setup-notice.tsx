export function AuthSetupNotice() {
  return <main className="auth-setup"><section><span>Server setup required</span><h1>Connect the school ID desk</h1><p>Add the Clerk, Liveblocks, and Upstash environment variables from <code>.env.example</code>, then restart the web app.</p></section></main>;
}
